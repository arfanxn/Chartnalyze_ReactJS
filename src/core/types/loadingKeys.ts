export type LoadingKeys =
    // User
    | 'self'
    | 'users.index'
    | 'users.show'
    | 'users.update'
    | 'users.destroy'

    // Notifications
    | 'notifications.index'
    | 'notifications.toggleRead'

    // Roles
    | 'roles.index'
    | 'roles.assignUser'

    // Permissions
    | 'permissions.index'

// there could be more
