export type LoadingKeys =
    // Dashboard
    | 'dashboard.index'

    // User
    | 'self'
    | 'users.index'
    | 'users.show'
    | 'users.update'
    | 'users.destroy'

    // Notifications
    | 'notifications.index'
    | 'notifications.toggleRead'

    // Roles
    | 'roles.index'
    | 'roles.assignUser'

    // Permissions
    | 'permissions.index'

    // Activities
    | 'activities.index'

// there could be more
